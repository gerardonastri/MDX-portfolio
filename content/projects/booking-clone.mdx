---
  title: Booking clone
  summary: A full-stack Booking clone built with NextJs.
  image: /images/projects/booking.png
  author: 'Gerardo Nastri'
  publishedAt: '2024-08-04'
---

This is a full-stack ecommerce store built with NextJs and Stripe. It uses the
`@stripe/stripe-js` package to handle payments and the `@next/bundle-analyzer`
package to analyze the bundle size.

## Features

- **Searching**: Searching hotels in specific destination.
- **Filtering**: Users can filter the results
- **User Authentication**: Allow users to sign up and log in.
- **Responsive Design**: the site is accessible from all platforms.

## Technologies

- **NextJs**: A React framework for building static and dynamic websites.
- **Vercel**: A cloud platform for static sites and serverless functions.

## Getting Started

To get started with this project, you can clone the repository and install the
dependencies:

```bash
git clone https://github.com/gerardonastri/jer-boking.git
cd jer-boking
npm install
```

Once the dependencies are installed, you can run the development server:

```bash
npm run dev
```

The development server will start at `http://localhost:3000`.

## Deployment

To deploy this project, you can use Vercel. You can link your GitHub repository
to Vercel and deploy the project with a single click.

## Conclusion

This booking clone is a great example of how you can build a full-stack
application with NextJs. 
